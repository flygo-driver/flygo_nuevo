import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Test base - RAI Driver', () {
    expect(
      1 + 1,
      equals(2),
    ); // Test simple para mantener el archivo activo sin errores
  });
}
