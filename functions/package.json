{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "test": "npm run build ; node --test test/taxista_cola_promote_logic.test.mjs",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint --ext .ts src"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^6.0.0",
    "nodemailer": "^8.0.7"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.4.0"
  },
  "type": "module"
}
